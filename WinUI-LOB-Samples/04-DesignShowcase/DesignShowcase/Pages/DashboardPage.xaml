<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DesignShowcase.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:DesignShowcase.Models"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Dashboard" />
        <TextBlock
            Grid.Row="1"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource BodyTextBlockStyle}"
            Text="At-a-glance summary of your line-of-business data." />

        <ItemsView
            Grid.Row="2"
            VerticalAlignment="Top"
            AutomationProperties.AutomationId="DashboardCards"
            AutomationProperties.Name="Summary cards"
            IsItemInvokedEnabled="False"
            ItemsSource="{x:Bind ViewModel.Cards, Mode=OneWay}"
            SelectionMode="None">
            <ItemsView.Layout>
                <UniformGridLayout
                    MinColumnSpacing="16"
                    MinItemWidth="220"
                    MinRowSpacing="16" />
            </ItemsView.Layout>
            <ItemsView.ItemTemplate>
                <DataTemplate x:DataType="models:SummaryCard">
                    <ItemContainer AutomationProperties.Name="{x:Bind Title}">
                        <Border
                            Padding="20"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource OverlayCornerRadius}">
                            <StackPanel Spacing="8">
                                <FontIcon
                                    HorizontalAlignment="Left"
                                    FontSize="24"
                                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                    Glyph="{x:Bind Glyph}" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind Title}" />
                                <TextBlock Style="{StaticResource TitleLargeTextBlockStyle}" Text="{x:Bind Value}" />
                            </StackPanel>
                        </Border>
                    </ItemContainer>
                </DataTemplate>
            </ItemsView.ItemTemplate>
        </ItemsView>
    </Grid>
</Page>
